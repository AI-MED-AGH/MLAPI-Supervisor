# MLAPI-Supervisor
ML-Supervisor is a centralized backend service built with FastAPI designed to bridge the gap between GitHub CI/CD and an on-premise single-node Kubernetes cluster. Handling authenticated image pulls from GHCR, managing deployment lifecycles, and providing real-time observability via webhooks to external monitoring apps.
